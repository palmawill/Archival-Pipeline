## Setup
You will need to separately install Lxml and BagIt for the pipeline to work. Run the following lines in your terminal to install:

## Install Lxml

> pip install lxml

## Install BagIt

> pip install bagit
